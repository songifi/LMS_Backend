 export enum DisclosureStatus {
    SUBMITTED = 'submitted',
    UNDER_REVIEW = 'under_review',
    CONFIRMED = 'confirmed',
    FIXED = 'fixed',
    REJECTED = 'rejected',
    PUBLISHED = 'published',
  }